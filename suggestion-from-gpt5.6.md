# bilibili-subtitle-ai 项目改进建议

> 基于当前项目源码、README 与架构文档整理。本文用于后续迭代参考，不直接修改实现。

## 一、总体判断

项目已经具备较完整的产品雏形：

- B 站官方字幕自动提取；
- 播放同步、高亮、滚动和跳转；
- 字幕上下文 AI 对话；
- 总结、要点、翻译等能力；
- DeepSeek-R1 深度思考模式；
- Markdown 安全渲染；
- 对话历史管理；
- SPA 视频/分 P 切换跟随；
- OpenAI 兼容 API；
- Service Worker 代理 API Key 和流式请求。

下一阶段重点不应只是继续增加按钮，而应优先提升：

1. 字幕获取覆盖率与稳定性；
2. 长字幕上下文处理效率；
3. 视频切换、并发请求和 Service Worker 生命周期可靠性；
4. AI 对话与观看过程的结合；
5. 测试、模块化、权限和隐私说明。

---

## 二、最高优先级改进

### 1. 增加无字幕视频的备用链路

当前项目主要依赖 B 站官方 CC / AI 字幕轨道，没有字幕的视频无法使用。建议按以下优先级构建备用方案：

```text
官方字幕
  ↓ 失败
其他可用 B 站字幕轨道
  ↓ 失败
用户导入本地字幕
  ↓ 可选
本地 Whisper 服务 / 浏览器端 Whisper
```

建议分阶段实现：

- 第一阶段：支持导入 `.srt`、`.vtt`、`.txt`；
- 第二阶段：支持调用 `localhost` Whisper 服务；
- 第三阶段：研究 Transformers.js / WebGPU 浏览器端转写。

隐私原则：不要默认上传音频到第三方服务，转写功能应明确提示数据去向，并优先支持本地处理。

### 2. 不要每次请求都发送完整字幕

当前 AI 请求会自动附带当前字幕全文。长视频会造成：

- Token 消耗大；
- 请求速度慢；
- 上下文可能超限；
- 简单问题也携带大量无关内容；
- 多轮对话上下文进一步膨胀。

建议采用分层上下文：

#### 短视频

直接发送全文。

#### 长视频

1. 将字幕按约 2,000～4,000 字符切块；
2. 保留 100～200 字符 overlap；
3. 根据问题关键词检索相关字幕块；
4. 附带当前播放位置附近的字幕；
5. 只发送最相关的若干块；
6. “总结全文”时采用专门的分块总结流程。

一个无需向量数据库的初版策略：

```text
当前播放位置前后各 90 秒
+ 关键词命中的字幕片段
+ 用户主动勾选的字幕行
```

后续再考虑 TF-IDF、BM25 或向量检索。

### 3. 防止视频切换造成旧请求覆盖新视频

SPA 切换、分 P 切换和多标签页场景容易出现竞态：

```text
视频 A 请求字幕
用户切换到视频 B
视频 B 请求字幕
视频 A 晚返回并覆盖视频 B
```

建议每次视频导航生成 `navigationToken` 或 `requestId`：

```js
const requestId = ++state.currentRequestId;
const result = await loadSubtitles(video);
if (requestId !== state.currentRequestId) return;
applySubtitles(result);
```

同时把以下内容纳入回归测试：

- bvid 改变；
- cid 改变但 bvid 不变；
- 视频元素被替换；
- 快速连续切换视频；
- 旧请求晚于新请求返回；
- 字幕获取失败后重新加载。

### 4. Service Worker 缓存不能只依赖内存

MV3 Service Worker 可能被浏览器挂起，内存 Map 会丢失。建议采用两层缓存：

- 热缓存：内存 Map；
- 持久缓存：`chrome.storage.local` 或 IndexedDB。

缓存 key 应至少包含：

```text
bvid + cid + trackId + 字幕版本或 hash
```

同时增加：

- TTL；
- LRU 淘汰；
- 最大缓存大小；
- 用户手动清理；
- 缓存命中/失效状态显示。

---

## 三、AI 交互体验改进

### 1. 增加“当前播放位置问答”

当前时间点应成为 AI 上下文的一等公民。用户提问时可以自动附带：

- 视频标题；
- 当前时间；
- 当前字幕句；
- 前后若干句；
- 当前章节（如果能获取）；
- 当前选中的字幕片段。

例如：

```text
我现在播放到 12:36，这里讲的“XX”是什么意思？
```

相比发送整段字幕，这更贴合实际观看过程。

### 2. 改进字幕选择交互

当前“单击加入上下文、双击跳转视频”不够直观，也容易误触。建议：

- 每行增加复选框；
- 单击行默认定位或选中，二者只保留一种主行为；
- 增加明确的“引用”按钮；
- 顶部显示已选上下文数量和字数；
- 支持清空、发送到输入框、直接询问；
- 支持连续区间选择；
- AI 回复中的引用可以点击跳回视频时间点。

建议展示：

```text
已选 3 段字幕 · 约 420 字
[清空] [发送到输入框] [询问 AI]
```

### 3. 增加视频类型 Prompt 预设

可以内置以下操作：

- 三句话总结；
- 详细笔记；
- 课程大纲；
- 技术要点；
- 关键术语解释；
- 时间线；
- 争议观点；
- 支持与反对论据；
- 生成 Quiz；
- 生成 Anki 卡片；
- 适合公众号/小红书的摘要。

### 4. 长视频总结采用分阶段处理

不要一次性把整部视频发送给模型，建议：

```text
字幕切块
  ↓
逐块提取要点
  ↓
合并章节摘要
  ↓
生成最终总结
```

界面展示进度：

```text
正在处理第 4 / 18 个片段
已完成约 22 分钟内容
[停止]
```

中间摘要可以缓存，支持失败后从中断位置继续。

### 5. 翻译能力扩展为双语字幕模式

当前已有英译功能，建议扩展为：

- 原文/译文上下排列；
- 原文/译文左右排列；
- 单句翻译；
- 自动识别语言；
- 翻译结果缓存；
- 用户术语表；
- 目标语言设置；
- 导出 SRT / VTT / TXT。

可支持：中文、英语、日语、韩语、法语、德语和自定义语言。

---

## 四、历史记录与数据设计

当前历史记录不保存完整字幕正文，而是按 bvid 关联。这个设计节省空间，但会有以下问题：

- 视频字幕之后可能变化；
- 视频可能被删除；
- 字幕接口可能失效；
- 用户在其他设备打开历史；
- 原回答无法还原当时的上下文。

建议历史记录至少保存：

- 视频标题；
- bvid；
- cid；
- 字幕轨道 ID；
- 字幕版本或 hash；
- 当前播放时间；
- 用户实际引用的字幕片段；
- AI 配置摘要；
- 完整回答。

不必保存全文，但应保存实际参与回答的上下文片段。

另外建议增加：

- JSON/Markdown 导出；
- 对话导入；
- 历史记录按视频分组；
- 收藏重要回答；
- 标签和搜索；
- 单条消息复制。

---

## 五、B 站接口与字幕链路

### 1. 将接口层从 `background.js` 中进一步拆分

建议逐步形成：

```text
bilibili/
├─ api-client.js
├─ wbi.js
├─ subtitle-api.js
├─ video-api.js
├─ retry.js
└─ cache.js
```

接口层统一返回项目自己的数据结构，避免 UI 直接依赖 B 站接口字段：

```js
{
  ok: true,
  video: { bvid, cid, title, page },
  tracks: []
}
```

这样 B 站 API 改动时只需修改适配层。

### 2. 按错误类型处理重试

建议区分：

| 错误 | 处理建议 |
|---|---|
| 网络超时 | 指数退避重试 |
| HTTP 412 | 延迟后重试，并提示可能触发风控 |
| 401/403 | 提示登录或 Cookie 无效 |
| 404 | 不重试，判断资源不存在 |
| 无字幕 | 不重试，进入备用链路 |
| JSON 结构变化 | 记录诊断信息，提示接口可能升级 |

不要让所有错误都使用相同的重试策略。

### 3. 字幕解析测试需要扩展

建议覆盖：

- 标准 JSON 字幕；
- 多语言轨道；
- 空字幕；
- 缺少 `from` / `to`；
- 时间戳为字符串；
- 时间戳乱序；
- 重叠字幕；
- HTML/特殊字符；
- 大量字幕行；
- 非 UTF-8 或异常文本；
- SRT/VTT 导入格式。

---

## 六、安全与隐私

### 1. API Key 设置体验

虽然 Key 只在 Service Worker 使用已经比放在页面上下文更安全，但建议：

- 默认隐藏 Key；
- 提供显示/隐藏按钮；
- 支持删除 Key；
- 不写入日志；
- 错误信息不回显请求头；
- 导出设置时默认排除 Key；
- 对第三方 Base URL 显示域名；
- 保存前校验 URL 协议为 HTTPS 或明确允许的 localhost。

设置页应明确提示：

> 使用第三方兼容 API 时，字幕和问题内容会发送到该服务商。

“插件本身不收集数据”和“用户配置的第三方服务会收到请求内容”需要区分说明。

### 2. Markdown 安全测试

继续覆盖：

- `javascript:`；
- `data:`；
- `vbscript:`；
- 图片链接；
- 自动链接；
- HTML 属性注入；
- SVG/Mermaid；
- 超长代码块；
- 恶意 Markdown 嵌套；
- 新窗口链接的 `noopener noreferrer`。

### 3. 权限说明

README 建议增加 manifest 权限表：

| 权限 | 用途 |
|---|---|
| `sidePanel` | 打开 AI 侧边栏 |
| `storage` | 保存设置、历史和缓存 |
| `cookies` | 使用 B 站登录态请求字幕 |
| host permission | 请求 B 站和 AI 接口 |

同时检查是否可以缩小 host permissions 范围，减少用户疑虑和商店审核压力。

---

## 七、工程质量和测试

### 1. 增加 CI

建议 GitHub Actions 执行：

```text
- node --check 所有 JS
- node test/smoke-test.js
- 校验 manifest.json
- 校验所有引用资源存在
- 检查版本号一致性
- 生成 zip artifact
```

README、manifest、badge 和压缩包名称不要各自维护版本号，最好建立统一版本来源。

### 2. 扩展冒烟测试范围

现有测试已经覆盖 MD5/WBI、Markdown XSS、资源完整性和消息链路。下一步补充：

- 字幕解析；
- 时间戳计算；
- 多语言格式；
- SSE 分片拼接；
- SSE 中断；
- AI 错误响应；
- 视频切换竞态；
- 多标签页跟随；
- Service Worker 重启；
- 历史记录迁移；
- storage 配额异常；
- 缓存过期和淘汰。

最优先是：**SSE 解析、字幕解析、视频切换竞态**。

### 3. 拆分 `sidepanel/panel.js`

功能增长后，建议拆为：

```text
sidepanel/
├─ panel.js
├─ state.js
├─ subtitle-list.js
├─ context-selection.js
├─ chat-view.js
├─ stream-client.js
├─ chat-history.js
├─ markdown-view.js
└─ panel.css
```

不必立刻引入 React 或构建工具，保持原生 JavaScript 也可以完成模块化。

### 4. 生命周期专项测试

针对 MV3、热重载、SPA 和多标签页，增加以下流程：

```text
load → charge/chat → reload → charge/chat → unload → reload
```

检查：

- 事件监听不会重复；
- 同一次请求只处理一次；
- 路由不会增长；
- timer 会清理；
- 旧 fiber/旧页面不会继续广播；
- 失败时旧数据不会覆盖新视频；
- Service Worker 重启后状态可恢复。

---

## 八、推荐优先级路线

### P0：可靠性

1. 抽出字幕和上下文处理的纯函数；
2. 增加字幕解析、SSE 和竞态测试；
3. 增加视频切换 `requestId` 防护；
4. 改进错误分类和重试；
5. 将关键缓存持久化；
6. 增加 API Key 和第三方服务隐私提示。

### P1：核心体验

1. 当前播放位置问答；
2. 字幕行复选/引用机制；
3. 长字幕分块和关键词检索；
4. 视频总结进度和断点恢复；
5. 双语字幕与 SRT/VTT 导出；
6. 历史记录保存实际引用片段。

### P2：覆盖率与产品化

1. 本地字幕导入；
2. 本地 Whisper 服务；
3. 视频章节和时间线；
4. 费用/Token 统计；
5. Prompt 预设；
6. Anki、Markdown、Notion 等导出。

### P3：工程完善

1. 拆分 `background.js` 和 `sidepanel/panel.js`；
2. 增加 GitHub Actions CI；
3. 统一版本号和发布包生成；
4. 增加权限说明和诊断页；
5. 建立 B 站接口适配层。

---

## 九、最推荐先做的三个改动

如果只选择三项，建议按这个顺序：

### 1. 长字幕分块检索

直接改善速度、成本和回答准确度，是最能提升日常使用体验的改动。

### 2. 当前播放位置问答 + 可点击时间戳

让 AI 真正融入观看流程，而不是单独的聊天窗口。

### 3. 视频切换竞态保护 + SSE/字幕解析测试

这是稳定性基础，能减少“字幕错位”“回答对应错视频”“流式输出异常”等难排查问题。

---

## 十、最终方向

项目可以从“B 站字幕 AI 助手”逐渐升级成：

> **面向视频学习、理解、检索和知识沉淀的浏览器工具。**

核心路径应是：

```text
发现视频
  ↓
获取或生成字幕
  ↓
按时间阅读与定位
  ↓
基于相关片段提问
  ↓
生成总结、笔记和翻译
  ↓
保存、导出并复用知识
```

重点不是继续增加零散功能，而是围绕这条主路径提升可靠性、上下文效率和知识沉淀能力。
